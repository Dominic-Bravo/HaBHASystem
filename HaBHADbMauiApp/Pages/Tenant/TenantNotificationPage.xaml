<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HaBHADbMauiApp.Pages.Tenant.TenantNotificationPage"
             Title="TenantNotificationPage" Background="White">
   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <VerticalStackLayout Padding="10">
            <Frame CornerRadius="50" WidthRequest="350" HeightRequest="50" BackgroundColor="Black" >
                <Label Text="Request List" HeightRequest="35" TextColor="White" HorizontalOptions="Center" Margin="5" FontAttributes="None" FontSize="20"/>
            </Frame>
        </VerticalStackLayout>

        <ScrollView Grid.Row="2">
            <ListView x:Name="TestListView" HasUnevenRows="True" SeparatorVisibility="None" SelectionMode="None" ItemTapped="TestListView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Frame MaximumHeightRequest="200" MinimumWidthRequest="100" BackgroundColor="LightGray" CornerRadius="40" Margin="10" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Label 
                                        Text="{Binding BookingDate}" 
                                        Grid.Row="0" 
                                        TextColor="Black"
                                        VerticalTextAlignment="Center" 
                                        HorizontalTextAlignment="Center" 
                                        FontAttributes="Bold" 
                                        FontSize="Title"/>
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Label 
                                            Text="{Binding ApprovalStatus}" 
                                            VerticalTextAlignment="Center" 
                                            TextColor="Black"
                                            Margin="0,10"
                                            HorizontalTextAlignment="Center" 
                                            Grid.Column="1" 
                                            FontSize="Subtitle" 
                                            LineBreakMode="WordWrap"/>

                                    </Grid>
                                </Grid>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>


        </ScrollView>
    </Grid>
</ContentPage>