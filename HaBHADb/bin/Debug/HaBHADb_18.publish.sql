/*
Deployment script for MauiDb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "MauiDb"
:setvar DefaultFilePrefix "MauiDb"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering Procedure [dbo].[ApproveOrRejectBooking]...';


GO
ALTER PROCEDURE ApproveOrRejectBooking
    @BookingId INT,
    @ApprovalStatus NVARCHAR(50), 
    @BoardinghouseId INT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE Bookings
    SET ApprovalStatus = @ApprovalStatus
    WHERE BookingId = @BookingId;

    IF @ApprovalStatus = 'Approved'
    BEGIN
        UPDATE BoardingHouses
        SET IsAvailble = 0 
        WHERE BoardinghouseId = @BoardinghouseId;
    END

    IF @ApprovalStatus = 'Rejected'
    BEGIN
        DELETE FROM Bookings
        WHERE BookingId = @BookingId;
    END
END;
GO
PRINT N'Altering Procedure [dbo].[GetTotalPriceForBoardingHouse]...';


GO
ALTER PROCEDURE [dbo].[GetTotalPriceForBoardingHouse]
    @MinimumPrice DECIMAL(18, 2),
    @MaximumPrice DECIMAL(18, 2)
AS
BEGIN
    SELECT 
        b.BoardinghouseId,
        b.RoomNumber,
        b.RoomSize,
        b.PricePerMonth,
        b.IsAvailble,
        b.Descriptions,
        ISNULL(SUM(a.Price), 0) AS TotalAmenitiesPrice, 
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) AS TotalPrice 
    FROM 
        dbo.BoardingHouses b
    LEFT JOIN 
        dbo.Amenities a ON b.BoardinghouseId = a.BoardinghouseId
    WHERE 
        b.IsAvailble = 0  
    GROUP BY 
        b.BoardinghouseId, 
        b.RoomNumber, 
        b.RoomSize, 
        b.PricePerMonth, 
        b.IsAvailble, 
        b.Descriptions
    HAVING 
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) BETWEEN @MinimumPrice AND @MaximumPrice; 
END
GO
PRINT N'Altering Procedure [dbo].[RemoveClientFromBoardingHouse]...';


GO
ALTER PROCEDURE RemoveClientFromBoardingHouse
    @BoardinghouseId INT,
    @ClientId NVARCHAR(450)
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE Bookings
    SET ClientId = NULL, ApprovalStatus = 'Cancelled'  
    WHERE BoardinghouseId = @BoardinghouseId AND ClientId = @ClientId AND ApprovalStatus = 'Pending';

    UPDATE BoardingHouses
    SET IsAvailble = 1
    WHERE BoardinghouseId = @BoardinghouseId;

    DELETE FROM ClientRequests
    WHERE BoardinghouseId = @BoardinghouseId AND ClientId = @ClientId;

    SELECT 'Client removed and boarding house marked as unavailable, ClientRequest deleted.' AS Message;
END;
GO
PRINT N'Creating Procedure [dbo].[GetAvailableBoardingHouses]...';


GO
CREATE PROCEDURE [dbo].[GetAvailableBoardingHouses]
AS
BEGIN
    SELECT 
        b.BoardinghouseId,
        b.RoomNumber,
        b.RoomSize,
        b.PricePerMonth,
        b.IsAvailble,
        b.Descriptions,
        b.ClientId,     
        b.TenantId,   
        ISNULL(SUM(a.Price), 0) AS TotalAmenitiesPrice, 
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) AS TotalPrice 
    FROM 
        dbo.BoardingHouses b
    LEFT JOIN 
        dbo.Amenities a ON b.BoardinghouseId = a.BoardinghouseId
    WHERE 
        b.IsAvailble = 1  
    GROUP BY 
        b.BoardinghouseId, 
        b.RoomNumber, 
        b.RoomSize, 
        b.PricePerMonth, 
        b.IsAvailble, 
        b.Descriptions,
        b.ClientId,     
        b.TenantId     
END
GO
PRINT N'Update complete.';


GO
