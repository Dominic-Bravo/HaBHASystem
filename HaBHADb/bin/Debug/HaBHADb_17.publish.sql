/*
Deployment script for MauiDb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "MauiDb"
:setvar DefaultFilePrefix "MauiDb"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[CreateQrImage]...';


GO
CREATE PROCEDURE [dbo].[CreateQrImage]
    @BoardingHouseId INT = NULL,    -- Optional parameter with default NULL
    @QrCodeImage NVARCHAR(MAX),
    @TenantId NVARCHAR(450) = NULL   -- Optional parameter with default NULL
AS
BEGIN
    -- Insert a new QR image record into the QrImages table
    INSERT INTO [dbo].[QrImages] (BoardingHouseId, QrCodeImage, TenantId)
    VALUES (@BoardingHouseId, @QrCodeImage, @TenantId);
    
    -- Return the ID of the newly created QR image
    SELECT SCOPE_IDENTITY() AS NewQrImageId;
END
GO
PRINT N'Creating Procedure [dbo].[DeleteQrImage]...';


GO
CREATE PROCEDURE [dbo].[DeleteQrImage]
    @QrImageId INT
AS
BEGIN
    DELETE FROM [dbo].[QrImages]
    WHERE QrImageId = @QrImageId;
    
    SELECT @@ROWCOUNT AS RowsAffected;
END
GO
PRINT N'Creating Procedure [dbo].[GetQrImagesByTenantId]...';


GO
CREATE PROCEDURE [dbo].[GetQrImagesByTenantId]
    @TenantId NVARCHAR(450)
AS
BEGIN
    SELECT QrImageId, BoardingHouseId, QrCodeImage, TenantId
    FROM [dbo].[QrImages]
    WHERE TenantId = @TenantId;
END
GO
PRINT N'Creating Procedure [dbo].[GetTotalPriceForBoardingHouse]...';


GO
CREATE PROCEDURE [dbo].[GetTotalPriceForBoardingHouse]
    @MinimumPrice DECIMAL(18, 2),
    @MaximumPrice DECIMAL(18, 2)
AS
BEGIN
    SELECT 
        b.BoardinghouseId,
        b.RoomNumber,
        b.RoomSize,
        b.PricePerMonth,
        b.IsAvailble,
        b.Descriptions,
        ISNULL(SUM(a.Price), 0) AS TotalAmenitiesPrice,  -- Sum of amenities price
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) AS TotalPrice  -- Total price
    FROM 
        dbo.BoardingHouses b
    LEFT JOIN 
        dbo.Amenities a ON b.BoardinghouseId = a.BoardinghouseId
    WHERE 
        b.IsAvailble = 0  -- Only include boarding houses where IsAvailable is 0 (not available)
    GROUP BY 
        b.BoardinghouseId, 
        b.RoomNumber, 
        b.RoomSize, 
        b.PricePerMonth, 
        b.IsAvailble, 
        b.Descriptions
    HAVING 
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) BETWEEN @MinimumPrice AND @MaximumPrice;  -- Filter by the total price range
END
GO
PRINT N'Creating Procedure [dbo].[GetTotalPriceForBoardingHousebyBhId]...';


GO
CREATE PROCEDURE [dbo].[GetTotalPriceForBoardingHousebyBhId]
    @BoardinghouseId INT
AS
BEGIN
    SELECT 
        b.BoardinghouseId,
        b.RoomNumber,
        b.RoomSize,
        b.PricePerMonth,
        b.IsAvailble,
        b.Descriptions,
        ISNULL(SUM(a.Price), 0) AS TotalAmenitiesPrice,  -- Sum of amenities price
        (b.PricePerMonth + ISNULL(SUM(a.Price), 0)) AS TotalPrice  -- Total price
    FROM 
        dbo.BoardingHouses b
    LEFT JOIN 
        dbo.Amenities a ON b.BoardinghouseId = a.BoardinghouseId
    WHERE 
        b.BoardinghouseId = @BoardinghouseId
    GROUP BY 
        b.BoardinghouseId, 
        b.RoomNumber, 
        b.RoomSize, 
        b.PricePerMonth, 
        b.IsAvailble, 
        b.Descriptions;
END
GO
PRINT N'Update complete.';


GO
