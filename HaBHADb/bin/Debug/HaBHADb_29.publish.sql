/*
Deployment script for MauiDb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "MauiDb"
:setvar DefaultFilePrefix "MauiDb"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS02\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering Procedure [dbo].[AddBooking]...';


GO
ALTER PROCEDURE AddBooking
    @BoardinghouseId INT,
    @ClientId NVARCHAR(450),
    @TenantId NVARCHAR(450),
    @CheckInDate DATETIME,
    @CheckOutDate DATETIME,
    @TotalAmount DECIMAL(18, 2),
    @Status NVARCHAR(50) = 'Pending'
AS
BEGIN
    SET NOCOUNT ON;

    INSERT INTO Bookings (BoardinghouseId, ClientId, TenantId, CheckInDate, CheckOutDate, TotalAmount, ApprovalStatus)
    VALUES (@BoardinghouseId, @ClientId, @TenantId, @CheckInDate, @CheckOutDate, @TotalAmount, @Status);

    SELECT SCOPE_IDENTITY() AS BookingId;
END;
GO
PRINT N'Creating Procedure [dbo].[GetTenantBoardingHouseCounts]...';


GO
CREATE PROCEDURE [dbo].[GetTenantBoardingHouseCounts]
AS
BEGIN
    SELECT 
        u.FirstName, 
        u.LastName, 
        u.ContactNumber,
        u.Email,
        u.Location,
        COUNT(b.BoardinghouseId) AS NumberOfBoardingHouses,
        COUNT(CASE WHEN b.IsAvailble = 1 THEN 1 END) AS AvailableBoardingHouses,
        COUNT(CASE WHEN b.IsAvailble = 0 THEN 1 END) AS NotAvailableBoardingHouses
    FROM 
        dbo.AspNetUsers u
    LEFT JOIN 
        dbo.BoardingHouses b ON u.Id = b.TenantId
    GROUP BY 
        u.Id, u.FirstName, u.LastName, u.ContactNumber, u.Email, u.Location;
END;
GO
PRINT N'Creating Procedure [dbo].[GetTenantBoardingHouseCountsByTenantId]...';


GO
CREATE PROCEDURE [dbo].[GetTenantBoardingHouseCountsByTenantId]
    @TenantId NVARCHAR(450)
AS
BEGIN
    SELECT 
        u.FirstName, 
        u.LastName, 
        u.ContactNumber,
        u.Email,
        u.Location,
        COUNT(b.BoardinghouseId) AS NumberOfBoardingHouses,
        COUNT(CASE WHEN b.IsAvailble = 1 THEN 1 END) AS AvailableBoardingHouses,
        COUNT(CASE WHEN b.IsAvailble = 0 THEN 1 END) AS NotAvailableBoardingHouses
    FROM 
        dbo.AspNetUsers u
    LEFT JOIN 
        dbo.BoardingHouses b ON u.Id = b.TenantId
    WHERE 
        u.Id = @TenantId 
    GROUP BY 
        u.Id, u.FirstName, u.LastName, u.ContactNumber, u.Email, u.Location;
END;
GO
PRINT N'Update complete.';


GO
